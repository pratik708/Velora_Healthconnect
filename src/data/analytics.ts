export const analytics = {
  // Google Tag Manager — loads GTM container (preferred over standalone GA4)
  gtmId: "",

  // Google Analytics 4 — standalone gtag.js (ignored if gtmId is set)
  ga4Id: "",

  // Meta (Facebook) Pixel
  metaPixelId: "",
} as const;
