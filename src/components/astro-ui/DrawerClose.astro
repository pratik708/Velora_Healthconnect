---
import { DrawerClose as ReactDrawerClose } from "@/components/ui/drawer";

interface Props {
  asChild?: boolean;
  class?: string;
}

const { class: className, ...rest } = Astro.props;
---

<ReactDrawerClose client:load className={className} {...rest}>
  <slot />
</ReactDrawerClose>
