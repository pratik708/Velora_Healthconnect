---
import { CommandList as ReactCommandList } from "@/components/ui/command";

interface Props {
  class?: string;
}

const { class: className, ...rest } = Astro.props;
---

<ReactCommandList client:visible className={className} {...rest}>
  <slot />
</ReactCommandList>
