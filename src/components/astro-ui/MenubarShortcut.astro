---
import { MenubarShortcut as ReactMenubarShortcut } from "@/components/ui/menubar";

interface Props {
  class?: string;
}

const { class: className, ...rest } = Astro.props;
---

<ReactMenubarShortcut client:visible className={className} {...rest}>
  <slot />
</ReactMenubarShortcut>
