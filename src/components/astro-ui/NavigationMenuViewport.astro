---
import { NavigationMenuViewport as ReactNavigationMenuViewport } from "@/components/ui/navigation-menu";

interface Props {
  class?: string;
  forceMount?: true;
}

const { class: className, ...rest } = Astro.props;
---

<ReactNavigationMenuViewport client:visible className={className} {...rest} />
