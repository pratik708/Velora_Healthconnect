---
import { CarouselContent as ReactCarouselContent } from "@/components/ui/carousel";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<ReactCarouselContent client:visible className={className}>
  <slot />
</ReactCarouselContent>
