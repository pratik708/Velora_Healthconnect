---
import { DialogTitle as ReactDialogTitle } from "@/components/ui/dialog";

interface Props {
  class?: string;
}

const { class: className, ...rest } = Astro.props;
---

<ReactDialogTitle client:load className={className} {...rest}>
  <slot />
</ReactDialogTitle>
