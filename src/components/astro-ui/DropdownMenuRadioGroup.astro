---
import { DropdownMenuRadioGroup as ReactDropdownMenuRadioGroup } from "@/components/ui/dropdown-menu";

interface Props {
  value?: string;
  onValueChange?: (value: string) => void;
}

const props = Astro.props;
---

<ReactDropdownMenuRadioGroup client:visible {...props}>
  <slot />
</ReactDropdownMenuRadioGroup>
