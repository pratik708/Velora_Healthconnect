---
import { CommandEmpty as ReactCommandEmpty } from "@/components/ui/command";

interface Props {
  class?: string;
}

const { class: className, ...rest } = Astro.props;
---

<ReactCommandEmpty client:visible className={className} {...rest}>
  <slot />
</ReactCommandEmpty>
