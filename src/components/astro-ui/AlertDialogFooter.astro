---
import { AlertDialogFooter as ReactAlertDialogFooter } from "@/components/ui/alert-dialog";

interface Props {
  class?: string;
}

const { class: className, ...rest } = Astro.props;
---

<ReactAlertDialogFooter client:load className={className} {...rest}>
  <slot />
</ReactAlertDialogFooter>
