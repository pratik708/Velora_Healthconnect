---
import { SheetDescription as ReactSheetDescription } from "@/components/ui/sheet";

interface Props {
  class?: string;
}

const { class: className, ...rest } = Astro.props;
---

<ReactSheetDescription client:load className={className} {...rest}>
  <slot />
</ReactSheetDescription>
