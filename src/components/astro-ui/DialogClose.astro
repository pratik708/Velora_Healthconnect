---
import { DialogClose as ReactDialogClose } from "@/components/ui/dialog";

interface Props {
  asChild?: boolean;
  class?: string;
}

const { class: className, ...rest } = Astro.props;
---

<ReactDialogClose client:load className={className} {...rest}>
  <slot />
</ReactDialogClose>
